import { Page, H1, H2, Text, Callout, Divider, CardGrid, Card } from '@site/src/components/Notion';
import { profiles } from '@site/src/data/profiles';

<Page>
  <H1>소셜아카이브</H1>
  <Text>인스타 · 카톡 · 유튜브 · 자료실 · 블로그 · 인프런 · 스토어를 한곳에서</Text>

  <Callout emoji="📂">
    무료 자료실, 블로그, 인프런 강의, 멤버십스토어까지 다양한 콘텐츠를 확인하세요.
  </Callout>

  <Divider />

  <H2>프로필 선택</H2>
  <CardGrid>
    {profiles.map((p) => (
      <Card
        key={p.id}
        href={p.href}
        to={p.to}
        icon={p.icon}
        title={p.title}
        description={p.description}
      />
    ))}
  </CardGrid>
</Page>
