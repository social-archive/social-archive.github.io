# Bachata Framebook 디자인 기준

이 프로젝트의 UI는 `reference/디지털 정부서비스 UIUX 가이드라인(2025.08).pdf`와 KRDS 공식 웹사이트를 기준으로 개발한다. 정부 기관의 식별 요소를 복제하지 않으며, 정보 구조·접근성·레이아웃·디자인 토큰 원칙을 전문적으로 따른다.

## 적용 우선순위

1. 접근성과 사용성
2. KRDS 시멘틱 토큰과 컴포넌트 역할
3. 반응형 레이아웃과 8-point 간격
4. Bachata Framebook 고유 콘텐츠와 브랜드 표현

## 색상

- Primitive 토큰은 `--krds-*`, 실제 UI 역할은 `--color-*` 시멘틱 토큰으로 사용한다.
- Gray는 배경·표면·본문·구분선, Primary는 주요 버튼·링크·선택 상태에만 사용한다.
- 배경/중립 60%, 보조 30%, 주요 상호작용 10% 비율을 기본으로 한다. Accent는 5% 이하로 제한한다.
- 기본 모드는 밝은 표면을 사용한다. 표면 계층은 gray-10 → gray-0 → gray-5 → gray-0 순서를 기준으로 구분선 또는 약한 그림자를 함께 사용한다.
- 일반 본문은 최소 4.5:1, 큰 텍스트와 UI 그래픽은 최소 3:1 대비를 확보한다. 상태는 색상만으로 전달하지 않는다.

## 반응형 레이아웃

- small: 360px 이상, 4 columns, gutter 16px, screen margin 16px
- medium: 768px 이상, 8 columns, gutter 16px, screen margin 24px
- large: 1024px 이상, 12 columns, gutter 24px, screen margin 24px
- xlarge: 1280px 이상, 12 columns, gutter 24px, screen margin 24px
- 360px 미만에서도 콘텐츠가 잘리지는 않아야 하지만 표준 최적화 기준은 360px부터다.
- 모바일 우선으로 작성하고, 큰 화면에서 기능과 정보 구조를 확장한다.

## 간격과 컴포넌트

- 기본 간격은 8px 배수(8, 16, 24, 32, 40, 64)를 우선 사용한다.
- 모든 핵심 터치 대상은 최소 44×44px로 제공하고 대상 사이에 충분한 간격을 둔다.
- 카드·입력·버튼은 동일한 radius와 시멘틱 토큰을 재사용한다.
- `focus-visible` 상태를 숨기지 않으며 키보드만으로 모든 기능을 사용할 수 있어야 한다.
- 모션은 `prefers-reduced-motion`을 존중한다.

## 변경 체크리스트

- 360px, 768px, 1024px, 1280px에서 레이아웃을 확인했는가?
- 본문·버튼·입력·선택 상태의 대비가 기준을 충족하는가?
- 모바일 화면 가장자리에서 최소 16px 여백을 유지하는가?
- 주요 액션에만 Primary를 사용했는가?
- 색상 없이도 선택·오류·상태를 이해할 수 있는가?
- 키보드 포커스, 대체 텍스트, 레이블이 제공되는가?
